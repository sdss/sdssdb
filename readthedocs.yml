version: 2

build:
  os: ubuntu-24.04
  tools:
    python: '3.12'

sphinx:
  configuration: docs/sphinx/conf.py

python:
  install:
    - requirements: docs/sphinx/requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - extras
        - dev
        - docs
